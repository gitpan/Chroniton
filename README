Please see the chroniton.pl manpage (or chroniton.pl --man) for the real
documentation.

Install this like you install any other module:

perl Makefile.PL
make
make test
make install

If the tests fail, then don't install the software -- file a bug
report instead.
